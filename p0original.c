#include <stdio.h>
int main()
{
  float base = 10.0;
  float altitude = 20.0;
  float area = 0.5 * base * altitude;
  printf("The area of the triangle with base = %f and altitude %f is %f\n",base, 
  altitude,area);
  return 0;
}